Task 6 — Contact Form with JavaScript Validation

This project is a simple Contact Form built using HTML, CSS, and JavaScript with client-side validation.  
It checks whether the user has correctly entered Name, Email, and Message, and displays helpful error or success messages.

 📌 Features

- ✔️ Validates all fields before submission  
- ✔️ Checks if fields are empty  
- ✔️ Valid email validation using Regex  
- ✔️ Shows error messages below each input  
- ✔️ Prevents form submission if data is incorrect  
- ✔️ Shows success message if all inputs are valid  
- ✔️ Reset button to clear form and errors  
- ✔️ Uses `event.preventDefault()` to stop default form submit  

🛠️ Technologies Used

- HTML— Form structure  
- CSS — Basic styling  
- JavaScript — Validation logic & user feedback  


📂 Project Files

contact-form.html
README.md

Everything is inside a single HTML file, including CSS and JavaScript.

🚀 How to Run

1. Download or clone the repository  
2. Open the file:

contact-form.html

3. It will open in your browser (Chrome recommended)

That's it — the form will work locally without a backend.







